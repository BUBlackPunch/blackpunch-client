import React from 'react';

import { SigninButton } from './SigninButton';

export default {
  title: 'sign_in',
  component: SigninButton,
};

export const Default = (): JSX.Element => <SigninButton />;
