import React from 'react';
import { Avatar } from 'evergreen-ui';
// import styled from '../../lib/styled';

function Profile(): JSX.Element {
  return <Avatar name="Fake Name" size={20} float="right" marginRight="1rem" />;
}

export default Profile;
